<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Информация о локациях</title>
    <style>
        .location-list {
            list-style-type: none;
            padding: 0;
            margin: 0;
            display: flex;
            background-color: #f0f0f0;
        }

        .location-list-item {
            margin: 5px;
            padding: 10px;
            cursor: pointer;
            border-radius: 5px;
        }

        .selected {
            background-color: #3498db;
            color: white;
        }

        #location-info {
            margin-top: 20px;
        }

        img {
            width: 100px; /* Укажите размер изображения по вашему усмотрению */
            /*height: 100px;*/
            margin-right: 5px;
        }
    </style>
</head>
<body>

    <ul class="location-list" id="locationList">
        <li class="location-list-item" onclick="showLocationInfo(this, 'path_to_image1')"> <img src="path_to_image1.jpg" alt="Баня"> </li>
        <li class="location-list-item" onclick="showLocationInfo(this, 'path_to_image2')"> <img src="path_to_image2.jpg" alt="Кухня"> </li>
        <li class="location-list-item" onclick="showLocationInfo(this, 'path_to_image3')"> <img src="path_to_image3.jpg" alt="Столовая"> </li>
        <li class="location-list-item" onclick="showLocationInfo(this, 'path_to_image4')"> <img src="path_to_image4.jpg" alt="Спальня"> </li>
        <li class="location-list-item" onclick="showLocationInfo(this, 'path_to_image5')"> <img src="/imag/10 Ridgeview Court.jpg" alt="Туалет"> </li>
    </ul>

    <div id="location-info"></div>

    <script>
        function showLocationInfo(element, location) {
            // Очистить выделение для всех элементов списка
            var items = document.querySelectorAll('.location-list-item');
            items.forEach(function (item) {
                item.classList.remove('selected');
            });

            // Выделить выбранный элемент
            element.classList.add('selected');

            // Отобразить информацию о выбранной локации
            var locationInfo = document.getElementById('location-info');
            locationInfo.innerHTML = `<h2>Информация о локации: ${location}</h2>`;
            // Дополнительная логика для отображения информации о выбранной локации
        }
    </script>
</body>
</html>
